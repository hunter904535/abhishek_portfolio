The actual resume PDF is served from `public/resume/` so it can be
linked and embedded directly without an import step. This folder is
kept here (per the requested project structure) in case you'd rather
import it as a module asset instead — just move the PDF back in and
update the import paths in `Resume.jsx` and `Hero.jsx`.
